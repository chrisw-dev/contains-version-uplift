# Format code first
npm run format
git add -u

# Run linter - if it fails, abort the commit
npm run lint

# Run tests - if they fail, abort the commit
npm test

# Rebuild dist/ and add it to the commit
npm run bundle
git add dist/
